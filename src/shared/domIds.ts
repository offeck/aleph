// DOM ids Aleph injects — single source so cross-bundle references (the
// content bundle's Aleph-authored mutation filter vs the mini-game overlay)
// can't drift.
export const MINI_GAME_OVERLAY_ID = "aleph-mini-game";
export const EDITOR_BIDI_STYLE_ID = "aleph-editor-bidi-styles";
