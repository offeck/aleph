{
  "name": "aleph",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Aleph — AI Chat Styler. Source of truth for the shipped version is manifest.json; keep equal.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rules": "firebase emulators:exec --project demo-aleph-test --only firestore \"vitest run --config tests/rules/vitest.config.ts\"",
    "deploy:rules": "npm run test:rules && firebase deploy --only firestore:rules --project aleph-ai-chat-styler",
    "lint": "eslint .",
    "check": "npm run typecheck && npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.16.0",
    "firebase": "^12.14.0",
    "firebase-tools": "^15.19.1",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.8"
  }
}
